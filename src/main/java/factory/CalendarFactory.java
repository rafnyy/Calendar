package factory;

import cal.Schedule;
import com.google.inject.assistedinject.Assisted;
import org.joda.time.Instant;
import users.Consultant;

public interface CalendarFactory {
//    Schedule create(Consultant consultant, @Assisted("startDate") Instant startDate, @Assisted("endDate") Instant endDate);
}
